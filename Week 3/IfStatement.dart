// if statement outputs depending on a certain conditional expression
void main() {
  var age = 30;
  if (age > 18) {
    print("Justine is a voter in Kenya");
  }
  //print("Justine is still young to vote");
}